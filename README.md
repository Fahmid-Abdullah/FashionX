# FashionX

An outfit recommendation project that learns user preferences over time using neural networks. Before using, you need to follow ReadMePlz.txt to get the images as the dataset it too large for github repo. If pushing, ignore your image folders to avoid errors.
